---
import FullLogo from './FullLogo.astro'
import IconLogo from './IconLogo.astro'
interface Props {
  variant?: 'full' | 'icon'
  size?: number
}

const { variant = 'full', size = 40 } = Astro.props
---

{variant === 'full' ? <FullLogo size={size} /> : <IconLogo size={size} />}
